env: "local"

http:
  host: "0.0.0.0"
  port: 8080
  timeout: 30s
  idle_timeout: 60s

storage: memory

db:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "postgres"
  dbname: "ots"
  sslmode: "disable"
